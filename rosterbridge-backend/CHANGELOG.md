# Changelog

## 2025-10-30

- Data layer: Student model changed to use Mongo ObjectID; added Connect/Disconnect helpers and graceful shutdown in server (B1).
- Data layer: Added StudentStore interface and mongo-backed store; handlers refactored to accept store (B2 work in progress).

(See commit history for full details.)
