# Example environment for local dev
# Copy to .env and adjust as needed

# Port for the Go API
PORT=8080

# Local MongoDB connection (Docker compose exposes 27017)
MONGODB_URI=mongodb://127.0.0.1:27017/?directConnection=true
